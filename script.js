document.getElementById('calculate').addEventListener('click', square)

function square () {
  const = parseInt(document.getElementById('input').value)
  let number = num * num
  document.querySelector('.answer').innerText = 'The Answer = ' + answer
}
